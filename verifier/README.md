# Verifier index

- `v1/` (2026-08-30): baseline acceptance criteria.
  Measures: (1) `python3 scripts/engine-test.py` → 85/85 pass; (2) `python3 -m py_compile` on all server/pulse/*.py → clean; (3) `node scripts/overall-test.mjs` → pass; (4) `npm run typecheck` → clean; (5) `npm test` → pass; (6) universe.json is valid non-empty JSON or engine tolerates empty without crash.
  Initial baseline. Run records appended under `verifier/runs/`.
- `v2/` (2026-08-30): adds `max_symbols_smoke.py` — 530-symbol offline drive of the real Pulse engine (config apply, indications scan, entries, stats write). Extends v1 with full-universe scale + x01 unlimited-cap assertions; v1 criteria unchanged.
- run 2026-08-30 (no-order-limit): order-count caps removed — entries_blocked() no longer gates on _order_est; burst throttle dropped; smoke asserts unlimited at order_est=500. All suites green (85/85, 11/11, 12/12).
- run 2026-08-30 (start-stop-pause): start/stop/pause hardened — sidecar apply_control with reset-eq + systemd ground-truth stats + heal_loop; engine reset-eq re-baseline + deposit rescue; desk control timeout 30s. Remote live tests on 152.53.114.112 (:3102/:3015) documented; all suites green (85/85, 191/0/4, 12/12, 45/45, tsc clean).
- run 2026-08-30 (defaults-dca-off): new defaults — DCA off by default (modules/engine/overlay/UI chain), historic lookback 240→960 bars, minPF 1.1→1.2 (set/coord/dca/exit + UI + overlays); vol1h default confirmed. Remote overlays applied live via sidecar :3015 + engines restarted (live green; vst drawdown-halt needs code deploy). All suites green (85/85, 191/0/4, 12/12, 45/45, tsc clean).
- run 2026-08-31 (conn-volume-progress): x01/x02 connection mixup diagnosed (x01 carries VST keys; real mainnet creds on record bingx-8581b0cb8581 — repair blocked on full key/secret, API masks them); impossible-volume fix (max_book_notional sane ceiling 32×base/64 USDT/25% room, entry 2×target guard, TARGET_NOTIONAL clamp 0.2..500, qa-book-cap invariant); calc progress bar with multi-details (engine Progress → sidecar lane_summary 13 fields → live-stats merge → LaneProgress UI). Full pulse_trader.py (233104B, sha 59e1bb43) restored to GitHub main + batch push. All suites green (85/85, 191/0/4, 12/12, 45/45, tsc clean).
- run 2026-08-31 (set-eval-positive-pf): set evaluation audited (last-N window math, cost-adjusted PF, avg calcs correct); 3 gating gaps fixed — proven-negative sets (last15 PF<1.00 over >=max(min_samples,8) trades) hard-deactivated, pick() tiers >=minPF -> >=1.00 -> discovery (unproven only) -> cold-start fallback (ungated only), entry_sense returns set-gate when all packs closed. 10 new in-process tests. Recorded in runs/2026-08-31-conn-volume-progress.txt section 4. Suites: 43/43 set, 85/85 engine-test (pre-always-start), tsc clean, npm 191/0, 45/45.
- run 2026-08-31 (always-start): live no-start-after-stop diagnosed (stale start-eq drawdown latch on boot, systemd start-limit-hit without reset-failed retry, no watchdog); always-start chain verified in-process — engine halt machine (reset-eq rescue, deposit rescue, auto-resume) + sidecar apply_control file contract + start retry, 11 new tests with negative controls. See runs/2026-08-31-always-start.txt. Suites: engine-test 96/96, set_engine 43/43, tsc clean, npm 191/0, overall-test 45/45. Remote deploy pending (update-linux.sh --force).
- run 2026-09-01 (control-coordination): start/pause/stop buttons hardened for rapid switching + stress — sidecar CONTROL_LOCK serialization (last click wins), heal_loop atomic check+start, engine ctrl-watch event wake (150ms), resume never restores pre-stop halt across explicit Start, UI per-action busy + always-clickable + pulse:control instant repoll, non-overlapping pollers. 12 new in-process tests. See runs/2026-09-01-control-coordination.txt. Suites: engine-test 108/108, set_engine 43/43, tsc clean, npm 191/0. pulse_trader restore patch regenerated (target 817f822b). Remote deploy pending.
- run 2026-09-01 (remote-ui-buttons): remote UI + control plane tested against 152.53.114.112 (:3102 UI via headless chromium, :3015 sidecar) — VST stop/start rc=0 + engine boots (cycle advancing), UI equity/open counts byte-match sidecar, progress fields present, no page errors; old-build Pause gating reproduced live (button disabled on stale stats — fixed in pushed build); VST trading still blocked by old-build drawdown latch until deploy. See runs/2026-09-01-remote-ui-buttons.txt.
- run 2026-09-01 (phantom-positions): dashboard showed 37 open positions while the exchange held none — root cause was adopt_exchange_positions' blanket "skip empty rest" guard skipping reconcile whenever the exchange was fully flat; fixed with a bounded 2-read confirm streak + stale-local sweep; stats/sidecar/UI now expose the real exchangeOpenCount next to the engine count (danger color on mismatch). 7 new in-process tests (116/116). Restore patch regenerated (target ad749ec3). See runs/2026-09-01-phantom-positions.txt. Follow-up: counts renamed Real (engine book) / Live (on exchange) + new Simulated stats (simOpenCount/simUPnl, system-internal calcs) across engine/sidecar/UI (121/121).
- run 2026-09-01 (live-defaults): the ACTUAL live-running remote overlay (139 keys, sidecar /config.json?conn=live) promoted to defaults everywhere — both repo overlays replaced by it; config-model DEFAULT_OVERLAY + fallbacks, modules strategy.dca, set/coord/dca/exit engine min_pf 1.1 + lookback 480, pulse_trader dca default-True chain + staggerS 0.6. Supersedes 2026-08-30 defaults-dca-off per explicit request. Suites 121/121, tsc clean, npm 191/0. Restore patch regenerated (target 24fb2fb2). See runs/2026-09-01-live-defaults.txt.
- run 2026-09-01 (block-counts-live-defaults): live settings re-verified (all block toggles ON, unlimited stack, ratio 1.0, pfRatio 0.8, defaultMinPF follows CTS real stage 1.2); Block coverage unlimited bug fixed (all 12 preview counts exposed + missing import), _coverage_blob sim_n NameError fixed, maybe_block_adds intern-PF floor now follows block.default_min_pf (was hardcoded 1.2), add-on volume parent reference now lane.base_qty (formula base) instead of a re-derived size_qty. 15 new in-process tests (137/137), tsc clean, npm build OK. Restore patch regenerated (target 2e39a63b). See runs/2026-09-01-block-counts-live-defaults.txt.
- run 2026-09-02 (strict-validated-gate): only VALIDATED (last15_n >= max(minSamples,8)) AND PROFITABLE (cost-PF >= 1.00) sets drive live orders — strict_gate default ON (overlay setStrictGate=false restores legacy discovery); discovery tier + ungated fallback + pack_open cold-pass + intern_any force-reopen all closed in strict mode. Per-kind indication gate (state/signals/active/direction/move/common): historic replay tag attribution (sig/ta/dir/move) + live Closed.ind_kind tape; proven losers cut, unproven kinds ride a validated pack (no cold-start deadlock). Block intern PF lifted off CTS floor only by validated+profitable sets, never below floor. 16 new tests — engine-test 153/153, set_engine self_test ok, tsc clean, npm build OK. Restore patch regenerated (target eb605459). See runs/2026-09-02-strict-validated-gate.txt. Remote deploy pending (update-linux.sh --force).
